name = "stock-watch-monitor"
compatibility_date = "2026-07-14"
main = "worker.mjs"

# 首次运行 `npm run deploy:cloudflare:auto` 会自动创建或复用 D1，
# 写入下面的绑定并执行远程迁移。不要把通知 Webhook 或 Token 写在这里。

[assets]
directory = "."
binding = "ASSETS"
run_worker_first = true

[triggers]
crons = ["*/5 * * * *"]
